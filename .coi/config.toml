[container]
image = "coi-oc"
alias = "oc"

[container.build]
base = "images:debian/13"
script = "build.sh"

[incus]
project = "default"

[network]
mode = "restricted"

[tool]
name = "claude"
permission_mode = "bypass"
auto_context = true
