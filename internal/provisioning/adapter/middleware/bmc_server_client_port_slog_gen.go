// Code generated by mockery. DO NOT EDIT.
// template: github.com/FuturFusion/operations-center/internal/util/logger/slog.gotmpl

package middleware

import (
	"context"
	"log/slog"

	"github.com/FuturFusion/operations-center/internal/provisioning"
	"github.com/FuturFusion/operations-center/internal/util/logger"
	"github.com/FuturFusion/operations-center/shared/api"
)

// BMCServerClientPortWithSlog implements provisioning.BMCServerClientPort that is instrumented with slog logger.
type BMCServerClientPortWithSlog struct {
	_base                 provisioning.BMCServerClientPort
	_isInformativeErrFunc func(error) bool
}

type BMCServerClientPortWithSlogOption func(s *BMCServerClientPortWithSlog)

func BMCServerClientPortWithSlogWithInformativeErrFunc(isInformativeErrFunc func(error) bool) BMCServerClientPortWithSlogOption {
	return func(_base *BMCServerClientPortWithSlog) {
		_base._isInformativeErrFunc = isInformativeErrFunc
	}
}

// NewBMCServerClientPortWithSlog instruments an implementation of the provisioning.BMCServerClientPort with simple logging.
func NewBMCServerClientPortWithSlog(base provisioning.BMCServerClientPort, opts ...BMCServerClientPortWithSlogOption) BMCServerClientPortWithSlog {
	this := BMCServerClientPortWithSlog{
		_base:                 base,
		_isInformativeErrFunc: func(error) bool { return false },
	}

	for _, opt := range opts {
		opt(&this)
	}

	return this
}

// GetServerDetails implements provisioning.BMCServerClientPort.
func (_d BMCServerClientPortWithSlog) GetServerDetails(ctx context.Context, server provisioning.Server) (bMCServerDetails api.BMCServerDetails, err error) {
	log := slog.With()
	if slog.Default().Enabled(ctx, logger.LevelTrace) {
		log = log.With(
			slog.Any("ctx", ctx),
			slog.Any("server", server),
		)
	}
	log.DebugContext(ctx, "=> calling GetServerDetails")
	defer func() {
		log := slog.With()
		if slog.Default().Enabled(ctx, logger.LevelTrace) {
			log = slog.With(
				slog.Any("bMCServerDetails", bMCServerDetails),
				slog.Any("err", err),
			)
		} else {
			if err != nil {
				log = slog.With("err", err)
			}
		}
		if err != nil {
			if _d._isInformativeErrFunc(err) {
				log.DebugContext(ctx, "<= method GetServerDetails returned an informative error")
			} else {
				log.ErrorContext(ctx, "<= method GetServerDetails returned an error")
			}
		} else {
			log.DebugContext(ctx, "<= method GetServerDetails finished")
		}
	}()
	return _d._base.GetServerDetails(ctx, server)
}

// Restart implements provisioning.BMCServerClientPort.
func (_d BMCServerClientPortWithSlog) Restart(ctx context.Context, server provisioning.Server, force bool) (bMCTaskMonitor *provisioning.BMCTaskMonitor, err error) {
	log := slog.With()
	if slog.Default().Enabled(ctx, logger.LevelTrace) {
		log = log.With(
			slog.Any("ctx", ctx),
			slog.Any("server", server),
			slog.Bool("force", force),
		)
	}
	log.DebugContext(ctx, "=> calling Restart")
	defer func() {
		log := slog.With()
		if slog.Default().Enabled(ctx, logger.LevelTrace) {
			log = slog.With(
				slog.Any("bMCTaskMonitor", bMCTaskMonitor),
				slog.Any("err", err),
			)
		} else {
			if err != nil {
				log = slog.With("err", err)
			}
		}
		if err != nil {
			if _d._isInformativeErrFunc(err) {
				log.DebugContext(ctx, "<= method Restart returned an informative error")
			} else {
				log.ErrorContext(ctx, "<= method Restart returned an error")
			}
		} else {
			log.DebugContext(ctx, "<= method Restart finished")
		}
	}()
	return _d._base.Restart(ctx, server, force)
}

// Start implements provisioning.BMCServerClientPort.
func (_d BMCServerClientPortWithSlog) Start(ctx context.Context, server provisioning.Server, force bool) (bMCTaskMonitor *provisioning.BMCTaskMonitor, err error) {
	log := slog.With()
	if slog.Default().Enabled(ctx, logger.LevelTrace) {
		log = log.With(
			slog.Any("ctx", ctx),
			slog.Any("server", server),
			slog.Bool("force", force),
		)
	}
	log.DebugContext(ctx, "=> calling Start")
	defer func() {
		log := slog.With()
		if slog.Default().Enabled(ctx, logger.LevelTrace) {
			log = slog.With(
				slog.Any("bMCTaskMonitor", bMCTaskMonitor),
				slog.Any("err", err),
			)
		} else {
			if err != nil {
				log = slog.With("err", err)
			}
		}
		if err != nil {
			if _d._isInformativeErrFunc(err) {
				log.DebugContext(ctx, "<= method Start returned an informative error")
			} else {
				log.ErrorContext(ctx, "<= method Start returned an error")
			}
		} else {
			log.DebugContext(ctx, "<= method Start finished")
		}
	}()
	return _d._base.Start(ctx, server, force)
}

// Stop implements provisioning.BMCServerClientPort.
func (_d BMCServerClientPortWithSlog) Stop(ctx context.Context, server provisioning.Server, force bool) (bMCTaskMonitor *provisioning.BMCTaskMonitor, err error) {
	log := slog.With()
	if slog.Default().Enabled(ctx, logger.LevelTrace) {
		log = log.With(
			slog.Any("ctx", ctx),
			slog.Any("server", server),
			slog.Bool("force", force),
		)
	}
	log.DebugContext(ctx, "=> calling Stop")
	defer func() {
		log := slog.With()
		if slog.Default().Enabled(ctx, logger.LevelTrace) {
			log = slog.With(
				slog.Any("bMCTaskMonitor", bMCTaskMonitor),
				slog.Any("err", err),
			)
		} else {
			if err != nil {
				log = slog.With("err", err)
			}
		}
		if err != nil {
			if _d._isInformativeErrFunc(err) {
				log.DebugContext(ctx, "<= method Stop returned an informative error")
			} else {
				log.ErrorContext(ctx, "<= method Stop returned an error")
			}
		} else {
			log.DebugContext(ctx, "<= method Stop finished")
		}
	}()
	return _d._base.Stop(ctx, server, force)
}

// WaitForTask implements provisioning.BMCServerClientPort.
func (_d BMCServerClientPortWithSlog) WaitForTask(ctx context.Context, server provisioning.Server, taskMonitor *provisioning.BMCTaskMonitor) (err error) {
	log := slog.With()
	if slog.Default().Enabled(ctx, logger.LevelTrace) {
		log = log.With(
			slog.Any("ctx", ctx),
			slog.Any("server", server),
			slog.Any("taskMonitor", taskMonitor),
		)
	}
	log.DebugContext(ctx, "=> calling WaitForTask")
	defer func() {
		log := slog.With()
		if slog.Default().Enabled(ctx, logger.LevelTrace) {
			log = slog.With(
				slog.Any("err", err),
			)
		} else {
			if err != nil {
				log = slog.With("err", err)
			}
		}
		if err != nil {
			if _d._isInformativeErrFunc(err) {
				log.DebugContext(ctx, "<= method WaitForTask returned an informative error")
			} else {
				log.ErrorContext(ctx, "<= method WaitForTask returned an error")
			}
		} else {
			log.DebugContext(ctx, "<= method WaitForTask finished")
		}
	}()
	return _d._base.WaitForTask(ctx, server, taskMonitor)
}
