// Code generated by mockery. DO NOT EDIT.
// template: github.com/FuturFusion/operations-center/internal/util/logger/slog.gotmpl

package middleware

import (
	"context"
	"log/slog"
	"time"

	"github.com/lxc/incus-os/incus-osd/api/images"

	"github.com/FuturFusion/operations-center/internal/provisioning"
	"github.com/FuturFusion/operations-center/internal/util/logger"
	"github.com/FuturFusion/operations-center/shared/api"
)

// componentSecureBootMediaPort identifies the log records of this decorator and allows the
// log level to be configured for it individually.
var componentSecureBootMediaPort = logger.RegisterComponent("provisioning.secure_boot_media_port")

// SecureBootMediaPortWithSlog implements provisioning.SecureBootMediaPort that is instrumented with slog logger.
type SecureBootMediaPortWithSlog struct {
	_base      provisioning.SecureBootMediaPort
	_component logger.Component
}

type SecureBootMediaPortWithSlogOption func(s *SecureBootMediaPortWithSlog)

// SecureBootMediaPortWithSlogWithComponent overrides the component this instance is
// attributed to. Use it to tell several instances of the same interface apart,
// e.g. the individual members of a chain.
func SecureBootMediaPortWithSlogWithComponent(component logger.Component) SecureBootMediaPortWithSlogOption {
	return func(_base *SecureBootMediaPortWithSlog) {
		_base._component = component
	}
}

// NewSecureBootMediaPortWithSlog instruments an implementation of the provisioning.SecureBootMediaPort with simple logging.
func NewSecureBootMediaPortWithSlog(base provisioning.SecureBootMediaPort, opts ...SecureBootMediaPortWithSlogOption) SecureBootMediaPortWithSlog {
	this := SecureBootMediaPortWithSlog{
		_base:      base,
		_component: componentSecureBootMediaPort,
	}

	for _, opt := range opts {
		opt(&this)
	}

	return this
}

// CheckSupported implements provisioning.SecureBootMediaPort.
func (_d SecureBootMediaPortWithSlog) CheckSupported(ctx context.Context, imageType api.ImageType, architecture images.UpdateFileArchitecture) (err error) {
	ctx = logger.ContextWithComponent(ctx, _d._component)
	log := slog.With()
	if slog.Default().Enabled(ctx, logger.LevelTrace) {
		log = log.With(
			slog.Any("ctx", ctx),
			slog.Any("imageType", imageType),
			slog.Any("architecture", architecture),
		)
	}
	log.DebugContext(ctx, "=> calling CheckSupported")
	defer func() {
		log := slog.With()
		if slog.Default().Enabled(ctx, logger.LevelTrace) {
			log = slog.With(
				slog.Any("err", err),
			)
		} else {
			if err != nil {
				log = slog.With("err", err)
			}
		}
		if err != nil {
			log.DebugContext(ctx, "<= method CheckSupported returned an error")
		} else {
			log.DebugContext(ctx, "<= method CheckSupported finished")
		}
	}()
	return _d._base.CheckSupported(ctx, imageType, architecture)
}

// Generate implements provisioning.SecureBootMediaPort.
func (_d SecureBootMediaPortWithSlog) Generate(ctx context.Context, imageType api.ImageType, architecture images.UpdateFileArchitecture, certificates provisioning.SecureBootCertificates) (s string, err error) {
	ctx = logger.ContextWithComponent(ctx, _d._component)
	log := slog.With()
	if slog.Default().Enabled(ctx, logger.LevelTrace) {
		log = log.With(
			slog.Any("ctx", ctx),
			slog.Any("imageType", imageType),
			slog.Any("architecture", architecture),
			slog.Any("certificates", certificates),
		)
	}
	log.DebugContext(ctx, "=> calling Generate")
	defer func() {
		log := slog.With()
		if slog.Default().Enabled(ctx, logger.LevelTrace) {
			log = slog.With(
				slog.String("s", s),
				slog.Any("err", err),
			)
		} else {
			if err != nil {
				log = slog.With("err", err)
			}
		}
		if err != nil {
			log.DebugContext(ctx, "<= method Generate returned an error")
		} else {
			log.DebugContext(ctx, "<= method Generate finished")
		}
	}()
	return _d._base.Generate(ctx, imageType, architecture, certificates)
}

// Open implements provisioning.SecureBootMediaPort.
func (_d SecureBootMediaPortWithSlog) Open(ctx context.Context, imageType api.ImageType, id string) (secureBootMediaImage *provisioning.SecureBootMediaImage, err error) {
	ctx = logger.ContextWithComponent(ctx, _d._component)
	log := slog.With()
	if slog.Default().Enabled(ctx, logger.LevelTrace) {
		log = log.With(
			slog.Any("ctx", ctx),
			slog.Any("imageType", imageType),
			slog.String("id", id),
		)
	}
	log.DebugContext(ctx, "=> calling Open")
	defer func() {
		log := slog.With()
		if slog.Default().Enabled(ctx, logger.LevelTrace) {
			log = slog.With(
				slog.Any("secureBootMediaImage", secureBootMediaImage),
				slog.Any("err", err),
			)
		} else {
			if err != nil {
				log = slog.With("err", err)
			}
		}
		if err != nil {
			log.DebugContext(ctx, "<= method Open returned an error")
		} else {
			log.DebugContext(ctx, "<= method Open finished")
		}
	}()
	return _d._base.Open(ctx, imageType, id)
}

// Prune implements provisioning.SecureBootMediaPort.
func (_d SecureBootMediaPortWithSlog) Prune(ctx context.Context, ttl time.Duration, inUse []string) (err error) {
	ctx = logger.ContextWithComponent(ctx, _d._component)
	log := slog.With()
	if slog.Default().Enabled(ctx, logger.LevelTrace) {
		log = log.With(
			slog.Any("ctx", ctx),
			slog.Duration("ttl", ttl),
			slog.Any("inUse", inUse),
		)
	}
	log.DebugContext(ctx, "=> calling Prune")
	defer func() {
		log := slog.With()
		if slog.Default().Enabled(ctx, logger.LevelTrace) {
			log = slog.With(
				slog.Any("err", err),
			)
		} else {
			if err != nil {
				log = slog.With("err", err)
			}
		}
		if err != nil {
			log.DebugContext(ctx, "<= method Prune returned an error")
		} else {
			log.DebugContext(ctx, "<= method Prune finished")
		}
	}()
	return _d._base.Prune(ctx, ttl, inUse)
}
