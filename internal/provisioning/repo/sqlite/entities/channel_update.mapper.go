// Code generated by generate-database from the incus project - DO NOT EDIT.

package entities

import (
	"context"
	"database/sql"
	"fmt"
	"strings"

	"github.com/google/uuid"

	"github.com/FuturFusion/operations-center/internal/provisioning"
)

var channelUpdateObjects = RegisterStmt(`
SELECT channels_updates.channel_id, channels_updates.update_id
  FROM channels_updates
  ORDER BY channels_updates.channel_id, channels_updates.update_id
`)

var channelUpdateObjectsByChannelID = RegisterStmt(`
SELECT channels_updates.channel_id, channels_updates.update_id
  FROM channels_updates
  WHERE ( channels_updates.channel_id = ? )
  ORDER BY channels_updates.channel_id, channels_updates.update_id
`)

var channelUpdateObjectsByUpdateID = RegisterStmt(`
SELECT channels_updates.channel_id, channels_updates.update_id
  FROM channels_updates
  WHERE ( channels_updates.update_id = ? )
  ORDER BY channels_updates.channel_id, channels_updates.update_id
`)

var channelUpdateObjectsByChannelIDAndUpdateID = RegisterStmt(`
SELECT channels_updates.channel_id, channels_updates.update_id
  FROM channels_updates
  WHERE ( channels_updates.channel_id = ? AND channels_updates.update_id = ? )
  ORDER BY channels_updates.channel_id, channels_updates.update_id
`)

var channelUpdateCreate = RegisterStmt(`
INSERT INTO channels_updates (channel_id, update_id)
  VALUES (?, ?)
`)

var channelUpdateDeleteByChannelID = RegisterStmt(`
DELETE FROM channels_updates WHERE channel_id = ?
`)

var channelUpdateDeleteByUpdateID = RegisterStmt(`
DELETE FROM channels_updates WHERE update_id = ?
`)

var channelUpdateDeleteByChannelIDAndUpdateID = RegisterStmt(`
DELETE FROM channels_updates WHERE channel_id = ? AND update_id = ?
`)

// channelUpdateColumns returns a string of column names to be used with a SELECT statement for the entity.
// Use this function when building statements to retrieve database entries matching the ChannelUpdate entity.
func channelUpdateColumns() string {
	return "channels_updates.channel_id, channels_updates.update_id"
}

// getChannelUpdates can be used to run handwritten sql.Stmts to return a slice of objects.
func getChannelUpdates(ctx context.Context, stmt *sql.Stmt, args ...any) ([]provisioning.ChannelUpdate, error) {
	objects := make([]provisioning.ChannelUpdate, 0)

	dest := func(scan func(dest ...any) error) error {
		c := provisioning.ChannelUpdate{}
		err := scan(&c.ChannelID, &c.UpdateID)
		if err != nil {
			return err
		}

		objects = append(objects, c)

		return nil
	}

	err := selectObjects(ctx, stmt, dest, args...)
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"channels_updates\" table: %w", err)
	}

	return objects, nil
}

// getChannelUpdatesRaw can be used to run handwritten query strings to return a slice of objects.
func getChannelUpdatesRaw(ctx context.Context, db dbtx, sql string, args ...any) ([]provisioning.ChannelUpdate, error) {
	objects := make([]provisioning.ChannelUpdate, 0)

	dest := func(scan func(dest ...any) error) error {
		c := provisioning.ChannelUpdate{}
		err := scan(&c.ChannelID, &c.UpdateID)
		if err != nil {
			return err
		}

		objects = append(objects, c)

		return nil
	}

	err := scan(ctx, db, sql, dest, args...)
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"channels_updates\" table: %w", err)
	}

	return objects, nil
}

// GetChannelUpdates returns all available Updates for the Channel.
// generator: channel_update GetMany
func GetChannelUpdates(ctx context.Context, db tx, channelID int) (_ []provisioning.Update, _err error) {
	defer func() {
		_err = mapErr(_err, "Channel_update")
	}()

	var err error

	// Result slice.
	objects := make([]provisioning.ChannelUpdate, 0)

	sqlStmt, err := Stmt(db, channelUpdateObjectsByChannelID)
	if err != nil {
		return nil, fmt.Errorf("Failed to get \"channelUpdateObjectsByChannelID\" prepared statement: %w", err)
	}

	args := []any{channelID}

	// Select.
	objects, err = getChannelUpdates(ctx, sqlStmt, args...)
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"channels_updates\" table: %w", err)
	}

	result := make([]provisioning.Update, len(objects))
	for i, object := range objects {
		update, err := GetUpdates(ctx, db, provisioning.UpdateFilter{ID: &object.UpdateID})
		if err != nil {
			return nil, err
		}

		result[i] = update[0]
	}

	return result, nil
}

// DeleteChannelUpdates deletes the channel_update matching the given key parameters.
// generator: channel_update DeleteMany
func DeleteChannelUpdates(ctx context.Context, db tx, channelID int) (_err error) {
	defer func() {
		_err = mapErr(_err, "Channel_update")
	}()

	stmt, err := Stmt(db, channelUpdateDeleteByChannelID)
	if err != nil {
		return fmt.Errorf("Failed to get \"channelUpdateDeleteByChannelID\" prepared statement: %w", err)
	}

	result, err := stmt.Exec(int(channelID))
	if err != nil {
		return fmt.Errorf("Delete \"channels_updates\" entry failed: %w", err)
	}

	_, err = result.RowsAffected()
	if err != nil {
		return fmt.Errorf("Fetch affected rows: %w", err)
	}

	return nil
}

// CreateChannelUpdates adds a new channel_update to the database.
// generator: channel_update Create
func CreateChannelUpdates(ctx context.Context, db tx, objects []provisioning.ChannelUpdate) (_err error) {
	defer func() {
		_err = mapErr(_err, "Channel_update")
	}()

	for _, object := range objects {
		args := make([]any, 2)

		// Populate the statement arguments.
		args[0] = object.ChannelID
		args[1] = object.UpdateID

		// Prepared statement to use.
		stmt, err := Stmt(db, channelUpdateCreate)
		if err != nil {
			return fmt.Errorf("Failed to get \"channelUpdateCreate\" prepared statement: %w", err)
		}

		// Execute the statement.
		_, err = stmt.Exec(args...)
		if err != nil && strings.HasPrefix(err.Error(), "UNIQUE constraint failed:") {
			return ErrConflict
		}

		if err != nil {
			return fmt.Errorf("Failed to create \"channels_updates\" entry: %w", err)
		}

	}

	return nil
}

// UpdateChannelUpdates updates the channel_update matching the given key parameters.
// generator: channel_update Update
func UpdateChannelUpdates(ctx context.Context, db tx, channelID int, updateUUIDs []uuid.UUID) (_err error) {
	defer func() {
		_err = mapErr(_err, "Channel_update")
	}()

	// Delete current entry.
	err := DeleteChannelUpdates(ctx, db, channelID)
	if err != nil {
		return err
	}

	// Get new entry IDs.
	channelUpdates := make([]provisioning.ChannelUpdate, 0, len(updateUUIDs))
	for _, entry := range updateUUIDs {
		refID, err := GetUpdateID(ctx, db, entry)
		if err != nil {
			return err
		}

		channelUpdates = append(channelUpdates, provisioning.ChannelUpdate{ChannelID: channelID, UpdateID: int(refID)})
	}

	err = CreateChannelUpdates(ctx, db, channelUpdates)
	if err != nil {
		return err
	}

	return nil
}

// GetUpdateChannels returns all available Channels for the Update.
// generator: channel_update GetMany
func GetUpdateChannels(ctx context.Context, db tx, updateID int) (_ []provisioning.Channel, _err error) {
	defer func() {
		_err = mapErr(_err, "Channel_update")
	}()

	var err error

	// Result slice.
	objects := make([]provisioning.ChannelUpdate, 0)

	sqlStmt, err := Stmt(db, channelUpdateObjectsByUpdateID)
	if err != nil {
		return nil, fmt.Errorf("Failed to get \"channelUpdateObjectsByUpdateID\" prepared statement: %w", err)
	}

	args := []any{updateID}

	// Select.
	objects, err = getChannelUpdates(ctx, sqlStmt, args...)
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"channels_updates\" table: %w", err)
	}

	result := make([]provisioning.Channel, len(objects))
	for i, object := range objects {
		channel, err := GetChannels(ctx, db, provisioning.ChannelFilter{ID: &object.ChannelID})
		if err != nil {
			return nil, err
		}

		result[i] = channel[0]
	}

	return result, nil
}

// DeleteUpdateChannels deletes the channel_update matching the given key parameters.
// generator: channel_update DeleteMany
func DeleteUpdateChannels(ctx context.Context, db tx, updateID int) (_err error) {
	defer func() {
		_err = mapErr(_err, "Channel_update")
	}()

	stmt, err := Stmt(db, channelUpdateDeleteByUpdateID)
	if err != nil {
		return fmt.Errorf("Failed to get \"channelUpdateDeleteByUpdateID\" prepared statement: %w", err)
	}

	result, err := stmt.Exec(int(updateID))
	if err != nil {
		return fmt.Errorf("Delete \"channels_updates\" entry failed: %w", err)
	}

	_, err = result.RowsAffected()
	if err != nil {
		return fmt.Errorf("Fetch affected rows: %w", err)
	}

	return nil
}

// CreateUpdateChannels adds a new channel_update to the database.
// generator: channel_update Create
func CreateUpdateChannels(ctx context.Context, db tx, objects []provisioning.ChannelUpdate) (_err error) {
	defer func() {
		_err = mapErr(_err, "Channel_update")
	}()

	for _, object := range objects {
		args := make([]any, 2)

		// Populate the statement arguments.
		args[0] = object.ChannelID
		args[1] = object.UpdateID

		// Prepared statement to use.
		stmt, err := Stmt(db, channelUpdateCreate)
		if err != nil {
			return fmt.Errorf("Failed to get \"channelUpdateCreate\" prepared statement: %w", err)
		}

		// Execute the statement.
		_, err = stmt.Exec(args...)
		if err != nil && strings.HasPrefix(err.Error(), "UNIQUE constraint failed:") {
			return ErrConflict
		}

		if err != nil {
			return fmt.Errorf("Failed to create \"channels_updates\" entry: %w", err)
		}

	}

	return nil
}

// UpdateUpdateChannels updates the channel_update matching the given key parameters.
// generator: channel_update Update
func UpdateUpdateChannels(ctx context.Context, db tx, updateID int, channelNames []string) (_err error) {
	defer func() {
		_err = mapErr(_err, "Channel_update")
	}()

	// Delete current entry.
	err := DeleteUpdateChannels(ctx, db, updateID)
	if err != nil {
		return err
	}

	// Get new entry IDs.
	channelUpdates := make([]provisioning.ChannelUpdate, 0, len(channelNames))
	for _, entry := range channelNames {
		refID, err := GetChannelID(ctx, db, entry)
		if err != nil {
			return err
		}

		channelUpdates = append(channelUpdates, provisioning.ChannelUpdate{UpdateID: updateID, ChannelID: int(refID)})
	}

	err = CreateUpdateChannels(ctx, db, channelUpdates)
	if err != nil {
		return err
	}

	return nil
}
