// Code generated by generate-inventory; DO NOT EDIT.

package sqlite

import (
	"context"
	"database/sql"
	"encoding/json"
	"fmt"
	"strings"

	"github.com/FuturFusion/operations-center/internal/domain"
	"github.com/FuturFusion/operations-center/internal/inventory"
	"github.com/FuturFusion/operations-center/internal/sqlite"
)

type storageBucket struct {
	db sqlite.DBTX
}

var _ inventory.StorageBucketRepo = &storageBucket{}

func NewStorageBucket(db sqlite.DBTX) *storageBucket {
	return &storageBucket{
		db: db,
	}
}

func (r storageBucket) Create(ctx context.Context, in inventory.StorageBucket) (inventory.StorageBucket, error) {
	const sqlStmt = `
WITH _server AS (
  SELECT clusters.name AS cluster_name FROM servers INNER JOIN clusters ON servers.cluster_id = clusters.id WHERE servers.name = :server_name
)
INSERT INTO storage_buckets (server_name, project_name, storage_pool_name, name, object, last_updated)
VALUES(:server_name, :project_name, :storage_pool_name, :name, :object, :last_updated)
RETURNING id, (SELECT cluster_name FROM _server) as cluster_name, server_name, project_name, storage_pool_name, name, object, last_updated;
`

	marshaledObject, err := json.Marshal(in.Object)
	if err != nil {
		return inventory.StorageBucket{}, err
	}

	row := r.db.QueryRowContext(ctx, sqlStmt,
		sql.Named("server_name", in.Server),
		sql.Named("project_name", in.ProjectName),
		sql.Named("storage_pool_name", in.StoragePoolName),
		sql.Named("name", in.Name),
		sql.Named("object", marshaledObject),
		sql.Named("last_updated", in.LastUpdated),
	)
	if row.Err() != nil {
		return inventory.StorageBucket{}, sqlite.MapErr(row.Err())
	}

	return scanStorageBucket(row)
}

func (r storageBucket) GetAllIDsWithFilter(ctx context.Context, filter inventory.StorageBucketFilter) ([]int, error) {
	const sqlStmt = `
SELECT storage_buckets.id
FROM storage_buckets
  INNER JOIN servers ON storage_buckets.server_name = servers.name
  INNER JOIN clusters ON servers.cluster_id = clusters.id
WHERE true
%s
ORDER BY storage_buckets.id
`

	var whereClause []string
	var args []any

	if filter.Cluster != nil {
		whereClause = append(whereClause, ` AND clusters.name = :cluster`)
		args = append(args, sql.Named("cluster", filter.Cluster))
	}

	if filter.Server != nil {
		whereClause = append(whereClause, ` AND servers.name = :server`)
		args = append(args, sql.Named("server", filter.Server))
	}

	if filter.Project != nil {
		whereClause = append(whereClause, ` AND storage_buckets.project_name = :project`)
		args = append(args, sql.Named("project", filter.Project))
	}

	sqlStmtComplete := fmt.Sprintf(sqlStmt, strings.Join(whereClause, " "))

	rows, err := r.db.QueryContext(ctx, sqlStmtComplete, args...)
	if err != nil {
		return nil, sqlite.MapErr(err)
	}

	defer func() { _ = rows.Close() }()

	var ids []int
	for rows.Next() {
		var id int
		err := rows.Scan(&id)
		if err != nil {
			return nil, sqlite.MapErr(err)
		}

		ids = append(ids, id)
	}

	if rows.Err() != nil {
		return nil, sqlite.MapErr(rows.Err())
	}

	return ids, nil
}

func (r storageBucket) GetByID(ctx context.Context, id int) (inventory.StorageBucket, error) {
	const sqlStmt = `
SELECT
  storage_buckets.id, clusters.name as cluster_name, storage_buckets.server_name, storage_buckets.project_name, storage_buckets.storage_pool_name, storage_buckets.name, storage_buckets.object, storage_buckets.last_updated
FROM
  storage_buckets
  INNER JOIN servers ON storage_buckets.server_name = servers.name
  INNER JOIN clusters ON servers.cluster_id = clusters.id
WHERE storage_buckets.id=:id;
`

	row := r.db.QueryRowContext(ctx, sqlStmt, sql.Named("id", id))
	if row.Err() != nil {
		return inventory.StorageBucket{}, sqlite.MapErr(row.Err())
	}

	return scanStorageBucket(row)
}

func (r storageBucket) DeleteByID(ctx context.Context, id int) error {
	const sqlStmt = `DELETE FROM storage_buckets WHERE id=:id;`

	result, err := r.db.ExecContext(ctx, sqlStmt, sql.Named("id", id))
	if err != nil {
		return sqlite.MapErr(err)
	}

	affectedRows, err := result.RowsAffected()
	if err != nil {
		return sqlite.MapErr(err)
	}

	if affectedRows == 0 {
		return domain.ErrNotFound
	}

	return nil
}

func (r storageBucket) DeleteByServer(ctx context.Context, server string) error {
	const sqlStmt = `DELETE FROM storage_buckets WHERE server_name=:server;`

	result, err := r.db.ExecContext(ctx, sqlStmt, sql.Named("server", server))
	if err != nil {
		return sqlite.MapErr(err)
	}

	affectedRows, err := result.RowsAffected()
	if err != nil {
		return sqlite.MapErr(err)
	}

	if affectedRows == 0 {
		return domain.ErrNotFound
	}

	return nil
}

func (r storageBucket) UpdateByID(ctx context.Context, in inventory.StorageBucket) (inventory.StorageBucket, error) {
	const sqlStmt = `
WITH _server AS (
  SELECT clusters.name AS cluster_name FROM servers INNER JOIN clusters ON servers.cluster_id=clusters.id WHERE servers.name=:server_name
)
UPDATE storage_buckets SET server_name=:server_name, project_name=:project_name, storage_pool_name=:storage_pool_name, name=:name, object=:object, last_updated=:last_updated
WHERE id=:id
RETURNING id, (SELECT cluster_name FROM _server) as cluster_name, server_name, project_name, storage_pool_name, name, object, last_updated;
`

	marshaledObject, err := json.Marshal(in.Object)
	if err != nil {
		return inventory.StorageBucket{}, err
	}

	row := r.db.QueryRowContext(ctx, sqlStmt,
		sql.Named("id", in.ID),
		sql.Named("server_name", in.Server),
		sql.Named("project_name", in.ProjectName),
		sql.Named("storage_pool_name", in.StoragePoolName),
		sql.Named("name", in.Name),
		sql.Named("object", marshaledObject),
		sql.Named("last_updated", in.LastUpdated),
	)
	if row.Err() != nil {
		return inventory.StorageBucket{}, sqlite.MapErr(row.Err())
	}

	return scanStorageBucket(row)
}

func scanStorageBucket(row interface{ Scan(dest ...any) error }) (inventory.StorageBucket, error) {
	var object []byte
	var storageBucket inventory.StorageBucket

	err := row.Scan(
		&storageBucket.ID,
		&storageBucket.Cluster,
		&storageBucket.Server,
		&storageBucket.ProjectName,
		&storageBucket.StoragePoolName,
		&storageBucket.Name,
		&object,
		&storageBucket.LastUpdated,
	)
	if err != nil {
		return inventory.StorageBucket{}, sqlite.MapErr(err)
	}

	err = json.Unmarshal(object, &storageBucket.Object)
	if err != nil {
		return inventory.StorageBucket{}, err
	}

	return storageBucket, nil
}
