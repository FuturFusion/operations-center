// Code generated by generate-database from the incus project - DO NOT EDIT.

package entities

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
	"time"

	"github.com/google/uuid"

	"github.com/FuturFusion/operations-center/internal/inventory"
)

var networkLoadBalancerObjects = RegisterStmt(`
SELECT network_load_balancers.id, network_load_balancers.uuid, clusters.name AS cluster, networks.project_name AS project_name, network_load_balancers.network_name, network_load_balancers.name, network_load_balancers.object, network_load_balancers.last_updated
  FROM network_load_balancers
  LEFT JOIN clusters ON network_load_balancers.cluster_id = clusters.id
  LEFT JOIN networks ON network_load_balancers.network_name = networks.name
  ORDER BY network_load_balancers.uuid
`)

var networkLoadBalancerObjectsByUUID = RegisterStmt(`
SELECT network_load_balancers.id, network_load_balancers.uuid, clusters.name AS cluster, networks.project_name AS project_name, network_load_balancers.network_name, network_load_balancers.name, network_load_balancers.object, network_load_balancers.last_updated
  FROM network_load_balancers
  LEFT JOIN clusters ON network_load_balancers.cluster_id = clusters.id
  LEFT JOIN networks ON network_load_balancers.network_name = networks.name
  WHERE ( network_load_balancers.uuid = ? )
  ORDER BY network_load_balancers.uuid
`)

var networkLoadBalancerNames = RegisterStmt(`
SELECT network_load_balancers.uuid
  FROM network_load_balancers
  ORDER BY network_load_balancers.uuid
`)

var networkLoadBalancerID = RegisterStmt(`
SELECT network_load_balancers.id FROM network_load_balancers
  WHERE network_load_balancers.uuid = ?
`)

var networkLoadBalancerCreate = RegisterStmt(`
INSERT INTO network_load_balancers (uuid, cluster_id, network_name, name, object, last_updated)
  VALUES (?, (SELECT clusters.id FROM clusters WHERE clusters.name = ?), ?, ?, ?, ?)
`)

var networkLoadBalancerUpdate = RegisterStmt(`
UPDATE network_load_balancers
  SET uuid = ?, cluster_id = (SELECT clusters.id FROM clusters WHERE clusters.name = ?), network_name = ?, name = ?, object = ?, last_updated = ?
 WHERE id = ?
`)

var networkLoadBalancerRename = RegisterStmt(`
UPDATE network_load_balancers SET name = ?, last_updated = ? WHERE uuid = ?
`)

var networkLoadBalancerDeleteByUUID = RegisterStmt(`
DELETE FROM network_load_balancers WHERE uuid = ?
`)

// GetNetworkLoadBalancerID return the ID of the network_load_balancer with the given key.
// generator: network_load_balancer ID
func GetNetworkLoadBalancerID(ctx context.Context, db tx, uuid uuid.UUID) (_ int64, _err error) {
	defer func() {
		_err = mapErr(_err, "Network_load_balancer")
	}()

	stmt, err := Stmt(db, networkLoadBalancerID)
	if err != nil {
		return -1, fmt.Errorf("Failed to get \"networkLoadBalancerID\" prepared statement: %w", err)
	}

	row := stmt.QueryRowContext(ctx, uuid)
	var id int64
	err = row.Scan(&id)
	if errors.Is(err, sql.ErrNoRows) {
		return -1, ErrNotFound
	}

	if err != nil {
		return -1, fmt.Errorf("Failed to get \"network_load_balancers\" ID: %w", err)
	}

	return id, nil
}

// NetworkLoadBalancerExists checks if a network_load_balancer with the given key exists.
// generator: network_load_balancer Exists
func NetworkLoadBalancerExists(ctx context.Context, db dbtx, uuid uuid.UUID) (_ bool, _err error) {
	defer func() {
		_err = mapErr(_err, "Network_load_balancer")
	}()

	stmt, err := Stmt(db, networkLoadBalancerID)
	if err != nil {
		return false, fmt.Errorf("Failed to get \"networkLoadBalancerID\" prepared statement: %w", err)
	}

	row := stmt.QueryRowContext(ctx, uuid)
	var id int64
	err = row.Scan(&id)
	if errors.Is(err, sql.ErrNoRows) {
		return false, nil
	}

	if err != nil {
		return false, fmt.Errorf("Failed to get \"network_load_balancers\" ID: %w", err)
	}

	return true, nil
}

// GetNetworkLoadBalancer returns the network_load_balancer with the given key.
// generator: network_load_balancer GetOne
func GetNetworkLoadBalancer(ctx context.Context, db dbtx, uuid uuid.UUID) (_ *inventory.NetworkLoadBalancer, _err error) {
	defer func() {
		_err = mapErr(_err, "Network_load_balancer")
	}()

	filter := inventory.NetworkLoadBalancerFilter{}
	filter.UUID = &uuid

	objects, err := GetNetworkLoadBalancers(ctx, db, filter)
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"network_load_balancers\" table: %w", err)
	}

	switch len(objects) {
	case 0:
		return nil, ErrNotFound
	case 1:
		return &objects[0], nil
	default:
		return nil, fmt.Errorf("More than one \"network_load_balancers\" entry matches")
	}
}

// networkLoadBalancerColumns returns a string of column names to be used with a SELECT statement for the entity.
// Use this function when building statements to retrieve database entries matching the NetworkLoadBalancer entity.
func networkLoadBalancerColumns() string {
	return "network_load_balancers.id, network_load_balancers.uuid, clusters.name AS cluster, networks.project_name AS project_name, network_load_balancers.network_name, network_load_balancers.name, network_load_balancers.object, network_load_balancers.last_updated"
}

// getNetworkLoadBalancers can be used to run handwritten sql.Stmts to return a slice of objects.
func getNetworkLoadBalancers(ctx context.Context, stmt *sql.Stmt, args ...any) ([]inventory.NetworkLoadBalancer, error) {
	objects := make([]inventory.NetworkLoadBalancer, 0)

	dest := func(scan func(dest ...any) error) error {
		n := inventory.NetworkLoadBalancer{}
		err := scan(&n.ID, &n.UUID, &n.Cluster, &n.ProjectName, &n.NetworkName, &n.Name, &n.Object, &n.LastUpdated)
		if err != nil {
			return err
		}

		objects = append(objects, n)

		return nil
	}

	err := selectObjects(ctx, stmt, dest, args...)
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"network_load_balancers\" table: %w", err)
	}

	return objects, nil
}

// getNetworkLoadBalancersRaw can be used to run handwritten query strings to return a slice of objects.
func getNetworkLoadBalancersRaw(ctx context.Context, db dbtx, sql string, args ...any) ([]inventory.NetworkLoadBalancer, error) {
	objects := make([]inventory.NetworkLoadBalancer, 0)

	dest := func(scan func(dest ...any) error) error {
		n := inventory.NetworkLoadBalancer{}
		err := scan(&n.ID, &n.UUID, &n.Cluster, &n.ProjectName, &n.NetworkName, &n.Name, &n.Object, &n.LastUpdated)
		if err != nil {
			return err
		}

		objects = append(objects, n)

		return nil
	}

	err := scan(ctx, db, sql, dest, args...)
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"network_load_balancers\" table: %w", err)
	}

	return objects, nil
}

// GetNetworkLoadBalancers returns all available network_load_balancers.
// generator: network_load_balancer GetMany
func GetNetworkLoadBalancers(ctx context.Context, db dbtx, filters ...inventory.NetworkLoadBalancerFilter) (_ []inventory.NetworkLoadBalancer, _err error) {
	defer func() {
		_err = mapErr(_err, "Network_load_balancer")
	}()

	var err error

	// Result slice.
	objects := make([]inventory.NetworkLoadBalancer, 0)

	// Pick the prepared statement and arguments to use based on active criteria.
	var sqlStmt *sql.Stmt
	args := []any{}
	queryParts := [2]string{}

	if len(filters) == 0 {
		sqlStmt, err = Stmt(db, networkLoadBalancerObjects)
		if err != nil {
			return nil, fmt.Errorf("Failed to get \"networkLoadBalancerObjects\" prepared statement: %w", err)
		}
	}

	for i, filter := range filters {
		if filter.UUID != nil && filter.Cluster == nil && filter.NetworkName == nil && filter.Name == nil {
			args = append(args, []any{filter.UUID}...)
			if len(filters) == 1 {
				sqlStmt, err = Stmt(db, networkLoadBalancerObjectsByUUID)
				if err != nil {
					return nil, fmt.Errorf("Failed to get \"networkLoadBalancerObjectsByUUID\" prepared statement: %w", err)
				}

				break
			}

			query, err := StmtString(networkLoadBalancerObjectsByUUID)
			if err != nil {
				return nil, fmt.Errorf("Failed to get \"networkLoadBalancerObjects\" prepared statement: %w", err)
			}

			parts := strings.SplitN(query, "ORDER BY", 2)
			if i == 0 {
				copy(queryParts[:], parts)
				continue
			}

			_, where, _ := strings.Cut(parts[0], "WHERE")
			queryParts[0] += "OR" + where
		} else if filter.UUID == nil && filter.Cluster == nil && filter.NetworkName == nil && filter.Name == nil {
			return nil, fmt.Errorf("Cannot filter on empty NetworkLoadBalancerFilter")
		} else {
			return nil, errors.New("No statement exists for the given Filter")
		}
	}

	// Select.
	if sqlStmt != nil {
		objects, err = getNetworkLoadBalancers(ctx, sqlStmt, args...)
	} else {
		queryStr := strings.Join(queryParts[:], "ORDER BY")
		objects, err = getNetworkLoadBalancersRaw(ctx, db, queryStr, args...)
	}

	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"network_load_balancers\" table: %w", err)
	}

	return objects, nil
}

// GetNetworkLoadBalancerNames returns the identifying field of network_load_balancer.
// generator: network_load_balancer GetNames
func GetNetworkLoadBalancerNames(ctx context.Context, db dbtx, filters ...inventory.NetworkLoadBalancerFilter) (_ []uuid.UUID, _err error) {
	defer func() {
		_err = mapErr(_err, "Network_load_balancer")
	}()

	var err error

	// Result slice.
	names := make([]uuid.UUID, 0)

	// Pick the prepared statement and arguments to use based on active criteria.
	var sqlStmt *sql.Stmt
	args := []any{}
	queryParts := [2]string{}

	if len(filters) == 0 {
		sqlStmt, err = Stmt(db, networkLoadBalancerNames)
		if err != nil {
			return nil, fmt.Errorf("Failed to get \"networkLoadBalancerNames\" prepared statement: %w", err)
		}
	}

	for _, filter := range filters {
		if filter.UUID == nil && filter.Cluster == nil && filter.NetworkName == nil && filter.Name == nil {
			return nil, fmt.Errorf("Cannot filter on empty NetworkLoadBalancerFilter")
		} else {
			return nil, errors.New("No statement exists for the given Filter")
		}
	}

	// Select.
	var rows *sql.Rows
	if sqlStmt != nil {
		rows, err = sqlStmt.QueryContext(ctx, args...)
	} else {
		queryStr := strings.Join(queryParts[:], "ORDER BY")
		rows, err = db.QueryContext(ctx, queryStr, args...)
	}

	if err != nil {
		return nil, err
	}

	defer func() { _ = rows.Close() }()
	for rows.Next() {
		var identifier uuid.UUID
		err := rows.Scan(&identifier)
		if err != nil {
			return nil, err
		}

		names = append(names, identifier)
	}

	err = rows.Err()
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"network_load_balancers\" table: %w", err)
	}

	return names, nil
}

// CreateNetworkLoadBalancer adds a new network_load_balancer to the database.
// generator: network_load_balancer Create
func CreateNetworkLoadBalancer(ctx context.Context, db dbtx, object inventory.NetworkLoadBalancer) (_ int64, _err error) {
	defer func() {
		_err = mapErr(_err, "Network_load_balancer")
	}()

	args := make([]any, 6)

	// Populate the statement arguments.
	args[0] = object.UUID
	args[1] = object.Cluster
	args[2] = object.NetworkName
	args[3] = object.Name
	args[4] = object.Object
	args[5] = time.Now().UTC().Format(time.RFC3339)

	// Prepared statement to use.
	stmt, err := Stmt(db, networkLoadBalancerCreate)
	if err != nil {
		return -1, fmt.Errorf("Failed to get \"networkLoadBalancerCreate\" prepared statement: %w", err)
	}

	// Execute the statement.
	result, err := stmt.Exec(args...)
	if err != nil && strings.HasPrefix(err.Error(), "UNIQUE constraint failed:") {
		return -1, ErrConflict
	}

	if err != nil {
		return -1, fmt.Errorf("Failed to create \"network_load_balancers\" entry: %w", err)
	}

	id, err := result.LastInsertId()
	if err != nil {
		return -1, fmt.Errorf("Failed to fetch \"network_load_balancers\" entry ID: %w", err)
	}

	return id, nil
}

// UpdateNetworkLoadBalancer updates the network_load_balancer matching the given key parameters.
// generator: network_load_balancer Update
func UpdateNetworkLoadBalancer(ctx context.Context, db tx, uuid uuid.UUID, object inventory.NetworkLoadBalancer) (_err error) {
	defer func() {
		_err = mapErr(_err, "Network_load_balancer")
	}()

	id, err := GetNetworkLoadBalancerID(ctx, db, uuid)
	if err != nil {
		return err
	}

	stmt, err := Stmt(db, networkLoadBalancerUpdate)
	if err != nil {
		return fmt.Errorf("Failed to get \"networkLoadBalancerUpdate\" prepared statement: %w", err)
	}

	result, err := stmt.Exec(object.UUID, object.Cluster, object.NetworkName, object.Name, object.Object, time.Now().UTC().Format(time.RFC3339), id)
	if err != nil {
		return fmt.Errorf("Update \"network_load_balancers\" entry failed: %w", err)
	}

	n, err := result.RowsAffected()
	if err != nil {
		return fmt.Errorf("Fetch affected rows: %w", err)
	}

	if n != 1 {
		return fmt.Errorf("Query updated %d rows instead of 1", n)
	}

	return nil
}

// RenameNetworkLoadBalancer renames the network_load_balancer matching the given key parameters.
// generator: network_load_balancer Rename
func RenameNetworkLoadBalancer(ctx context.Context, db dbtx, uuid uuid.UUID, to string) (_err error) {
	defer func() {
		_err = mapErr(_err, "Network_load_balancer")
	}()

	stmt, err := Stmt(db, networkLoadBalancerRename)
	if err != nil {
		return fmt.Errorf("Failed to get \"networkLoadBalancerRename\" prepared statement: %w", err)
	}

	result, err := stmt.Exec(to, time.Now().UTC().Format(time.RFC3339), uuid)
	if err != nil {
		return fmt.Errorf("Rename NetworkLoadBalancer failed: %w", err)
	}

	n, err := result.RowsAffected()
	if err != nil {
		return fmt.Errorf("Fetch affected rows failed: %w", err)
	}

	if n != 1 {
		return fmt.Errorf("Query affected %d rows instead of 1", n)
	}

	return nil
}

// DeleteNetworkLoadBalancer deletes the network_load_balancer matching the given key parameters.
// generator: network_load_balancer DeleteOne-by-UUID
func DeleteNetworkLoadBalancer(ctx context.Context, db dbtx, uuid uuid.UUID) (_err error) {
	defer func() {
		_err = mapErr(_err, "Network_load_balancer")
	}()

	stmt, err := Stmt(db, networkLoadBalancerDeleteByUUID)
	if err != nil {
		return fmt.Errorf("Failed to get \"networkLoadBalancerDeleteByUUID\" prepared statement: %w", err)
	}

	result, err := stmt.Exec(uuid)
	if err != nil {
		return fmt.Errorf("Delete \"network_load_balancers\": %w", err)
	}

	n, err := result.RowsAffected()
	if err != nil {
		return fmt.Errorf("Fetch affected rows: %w", err)
	}

	if n == 0 {
		return ErrNotFound
	} else if n > 1 {
		return fmt.Errorf("Query deleted %d NetworkLoadBalancer rows instead of 1", n)
	}

	return nil
}
