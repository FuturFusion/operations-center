// Code generated by generate-inventory; DO NOT EDIT.

package inventory

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/expr-lang/expr"
	"github.com/expr-lang/expr/vm"
	"github.com/google/uuid"

	"github.com/FuturFusion/operations-center/internal/domain"
	"github.com/FuturFusion/operations-center/internal/transaction"
)

type storagePoolService struct {
	repo              StoragePoolRepo
	clusterSvc        ProvisioningClusterService
	storagePoolClient StoragePoolServerClient

	clusterSyncFilterFunc func(storagePool StoragePool) bool

	now func() time.Time
}

var _ StoragePoolService = &storagePoolService{}

type StoragePoolServiceOption func(s *storagePoolService)

func StoragePoolWithSyncFilter(clusterSyncFilterFunc func(storagePool StoragePool) bool) StoragePoolServiceOption {
	return func(s *storagePoolService) {
		s.clusterSyncFilterFunc = clusterSyncFilterFunc
	}
}

func NewStoragePoolService(repo StoragePoolRepo, clusterSvc ProvisioningClusterService, client StoragePoolServerClient, opts ...StoragePoolServiceOption) storagePoolService {
	storagePoolSvc := storagePoolService{
		repo:              repo,
		clusterSvc:        clusterSvc,
		storagePoolClient: client,

		clusterSyncFilterFunc: func(storagePool StoragePool) bool {
			return false
		},

		now: time.Now,
	}

	for _, opt := range opts {
		opt(&storagePoolSvc)
	}

	return storagePoolSvc
}

func (s storagePoolService) GetAllWithFilter(ctx context.Context, filter StoragePoolFilter) (StoragePools, error) {
	var filterExpression *vm.Program
	var err error

	if filter.Expression != nil {
		filterExpression, err = expr.Compile(*filter.Expression, []expr.Option{expr.Env(ToExprStoragePool(StoragePool{}))}...)
		if err != nil {
			return nil, err
		}
	}

	storagePools, err := s.repo.GetAllWithFilter(ctx, filter)
	if err != nil {
		return nil, err
	}

	var filteredStoragePools StoragePools
	if filter.Expression != nil {
		for _, storagePool := range storagePools {
			output, err := expr.Run(filterExpression, ToExprStoragePool(storagePool))
			if err != nil {
				return nil, err
			}

			result, ok := output.(bool)
			if !ok {
				return nil, fmt.Errorf("Filter expression %q does not evaluate to boolean result: %v", *filter.Expression, output)
			}

			if result {
				filteredStoragePools = append(filteredStoragePools, storagePool)
			}
		}

		return filteredStoragePools, nil
	}

	return storagePools, nil
}

func (s storagePoolService) GetAllUUIDsWithFilter(ctx context.Context, filter StoragePoolFilter) ([]uuid.UUID, error) {
	var filterExpression *vm.Program
	var err error

	type Env struct {
		UUID string `expr:"uuid"`
	}

	if filter.Expression != nil {
		filterExpression, err = expr.Compile(*filter.Expression, []expr.Option{expr.Env(Env{})}...)
		if err != nil {
			return nil, err
		}
	}

	storagePoolsUUIDs, err := s.repo.GetAllUUIDsWithFilter(ctx, filter)
	if err != nil {
		return nil, err
	}

	var filteredStoragePoolsUUIDs []uuid.UUID
	if filter.Expression != nil {
		for _, storagePoolUUID := range storagePoolsUUIDs {
			output, err := expr.Run(filterExpression, Env{storagePoolUUID.String()})
			if err != nil {
				return nil, err
			}

			result, ok := output.(bool)
			if !ok {
				return nil, fmt.Errorf("Filter expression %q does not evaluate to boolean result: %v", *filter.Expression, output)
			}

			if result {
				filteredStoragePoolsUUIDs = append(filteredStoragePoolsUUIDs, storagePoolUUID)
			}
		}

		return filteredStoragePoolsUUIDs, nil
	}

	return storagePoolsUUIDs, nil
}

func (s storagePoolService) GetByUUID(ctx context.Context, id uuid.UUID) (StoragePool, error) {
	return s.repo.GetByUUID(ctx, id)
}

func (s storagePoolService) ResyncByUUID(ctx context.Context, id uuid.UUID) error {
	err := transaction.Do(ctx, func(ctx context.Context) error {
		storagePool, err := s.repo.GetByUUID(ctx, id)
		if err != nil {
			return err
		}

		endpoint, err := s.clusterSvc.GetEndpoint(ctx, storagePool.Cluster)
		if err != nil {
			return err
		}

		retrievedStoragePool, err := s.storagePoolClient.GetStoragePoolByName(ctx, endpoint, storagePool.Name)
		if errors.Is(err, domain.ErrNotFound) {
			err = s.repo.DeleteByUUID(ctx, storagePool.UUID)
			if err != nil {
				return fmt.Errorf(`Failed to delete storage_pool %q from cluster %q: %w`, storagePool.UUID.String(), storagePool.Cluster, err)
			}

			return nil
		}

		if err != nil {
			return err
		}

		storagePool.Object = IncusStoragePoolWrapper{retrievedStoragePool}
		storagePool.LastUpdated = s.now()
		storagePool.DeriveUUID()

		err = storagePool.Validate()
		if err != nil {
			return err
		}

		_, err = s.repo.UpdateByUUID(ctx, storagePool)
		if err != nil {
			return fmt.Errorf(`Failed to update storage_pool %q for cluster %q: %w`, storagePool.UUID.String(), storagePool.Cluster, err)
		}

		return nil
	})
	if err != nil {
		return err
	}

	return nil
}

func (s storagePoolService) ResyncByName(ctx context.Context, clusterName string, event domain.LifecycleEvent) error {
	if event.ResourceType != "storage-pool" {
		return nil
	}

	var err error
	switch event.Operation {
	case domain.LifecycleOperationCreate:
		err = s.handleCreateEvent(ctx, clusterName, event)

	case domain.LifecycleOperationDelete:
		err = s.handleDeleteEvent(ctx, clusterName, event)

	case domain.LifecycleOperationRename:
		err = s.handleRenameEvent(ctx, clusterName, event)

	case domain.LifecycleOperationUpdate:
		err = s.handleUpdateEvent(ctx, clusterName, event)

	default:
		err = fmt.Errorf("Invalid lifecycle operation %q", event.Operation)
	}

	if err != nil {
		if errors.Is(err, domain.ErrNotFound) {
			// Ignore "not found" errors.
			return nil
		}

		return fmt.Errorf("Failed to handle lifecycle event: %w", err)
	}

	return nil
}

func (s storagePoolService) handleCreateEvent(ctx context.Context, clusterName string, event domain.LifecycleEvent) error {
	endpoint, err := s.clusterSvc.GetEndpoint(ctx, clusterName)
	if err != nil {
		return err
	}

	retrievedStoragePool, err := s.storagePoolClient.GetStoragePoolByName(ctx, endpoint, event.Source.Name)
	if err != nil {
		return err
	}

	storagePool := StoragePool{
		Cluster:     clusterName,
		Name:        retrievedStoragePool.Name,
		Object:      IncusStoragePoolWrapper{retrievedStoragePool},
		LastUpdated: s.now(),
	}

	storagePool.DeriveUUID()

	if s.clusterSyncFilterFunc(storagePool) {
		return nil
	}

	err = storagePool.Validate()
	if err != nil {
		return err
	}

	_, err = s.repo.Create(ctx, storagePool)
	if err != nil {
		return fmt.Errorf(`Failed to create storage_pool %q for cluster %q: %w`, storagePool.UUID.String(), storagePool.Cluster, err)
	}

	return nil
}

func (s storagePoolService) handleDeleteEvent(ctx context.Context, clusterName string, event domain.LifecycleEvent) error {
	UUIDs, err := s.repo.GetAllUUIDsWithFilter(ctx, StoragePoolFilter{
		Cluster: &clusterName,
		Name:    &event.Source.Name,
	})
	if err != nil {
		return err
	}

	var errs []error
	for _, UUID := range UUIDs {
		err = s.repo.DeleteByUUID(ctx, UUID)
		if err != nil {
			err = fmt.Errorf(`Failed to delete storage_pool %q from cluster %q: %w`, UUID.String(), clusterName, err)
			errs = append(errs, err)
		}
	}

	err = errors.Join(errs...)
	if err != nil {
		return fmt.Errorf("Failed to remove storage_pool by name: %w", errors.Join(errs...))
	}

	return nil
}

func (s storagePoolService) handleRenameEvent(ctx context.Context, clusterName string, event domain.LifecycleEvent) error {
	deleteEvent := event
	deleteEvent.Source.Name = deleteEvent.Source.OldName

	var errs []error
	err := s.handleDeleteEvent(ctx, clusterName, deleteEvent)
	if err != nil {
		errs = append(errs,
			fmt.Errorf(`Failed to delete storage_pool %q from cluster %q: %w`, deleteEvent.Source.Name, clusterName, err),
		)
	}

	err = s.handleCreateEvent(ctx, clusterName, event)
	if err != nil {
		errs = append(errs,
			fmt.Errorf(`Failed to create storage_pool %q for cluster %q: %w`, event.Source.Name, clusterName, err),
		)
	}

	err = errors.Join(errs...)
	if err != nil {
		return err
	}

	return nil
}

func (s storagePoolService) handleUpdateEvent(ctx context.Context, clusterName string, event domain.LifecycleEvent) error {
	UUIDs, err := s.repo.GetAllUUIDsWithFilter(ctx, StoragePoolFilter{
		Cluster: &clusterName,
		Name:    &event.Source.Name,
	})
	if err != nil {
		return err
	}

	if len(UUIDs) == 0 {
		return s.handleCreateEvent(ctx, clusterName, event)
	}

	var errs []error
	for _, UUID := range UUIDs {
		err := s.ResyncByUUID(ctx, UUID)
		if err != nil {
			err = fmt.Errorf(`Failed to resync storage_pool %q for cluster %q: %w`, UUID.String(), clusterName, err)
			errs = append(errs, err)
		}
	}

	err = errors.Join(errs...)
	if err != nil {
		return fmt.Errorf("Failed to resync storage_pool by name: %w", errors.Join(errs...))
	}

	return nil
}

func (s storagePoolService) SyncCluster(ctx context.Context, name string) error {
	endpoint, err := s.clusterSvc.GetEndpoint(ctx, name)
	if err != nil {
		return err
	}

	retrievedStoragePools, err := s.storagePoolClient.GetStoragePools(ctx, endpoint)
	if err != nil {
		return err
	}

	err = transaction.Do(ctx, func(ctx context.Context) error {
		err = s.repo.DeleteWithFilter(ctx, StoragePoolFilter{
			Cluster: &name,
		})
		if err != nil && !errors.Is(err, domain.ErrNotFound) {
			return fmt.Errorf(`Failed to delete "storage_pool" from cluster %q: %w`, name, err)
		}

		for _, retrievedStoragePool := range retrievedStoragePools {
			storagePool := StoragePool{
				Cluster:     name,
				Name:        retrievedStoragePool.Name,
				Object:      IncusStoragePoolWrapper{retrievedStoragePool},
				LastUpdated: s.now(),
			}

			storagePool.DeriveUUID()

			if s.clusterSyncFilterFunc(storagePool) {
				continue
			}

			err = storagePool.Validate()
			if err != nil {
				return err
			}

			_, err := s.repo.Create(ctx, storagePool)
			if err != nil {
				return fmt.Errorf(`Failed to create storage_pool %q for cluster %q: %w`, storagePool.UUID.String(), name, err)
			}
		}

		return nil
	})
	if err != nil {
		return err
	}

	return nil
}
