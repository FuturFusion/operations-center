// Code generated by generate-database from the incus project - DO NOT EDIT.

package entities

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
	"time"

	"github.com/FuturFusion/operations-center/internal/image"
)

var incusImageObjects = RegisterStmt(`
SELECT incus_images.id, incus_images.name, incus_images.aliases, incus_images.description, incus_images.operating_system, incus_images.release, incus_images.architecture, incus_images.variant, incus_images.versions, incus_images.last_updated
  FROM incus_images
  ORDER BY incus_images.name
`)

var incusImageObjectsByID = RegisterStmt(`
SELECT incus_images.id, incus_images.name, incus_images.aliases, incus_images.description, incus_images.operating_system, incus_images.release, incus_images.architecture, incus_images.variant, incus_images.versions, incus_images.last_updated
  FROM incus_images
  WHERE ( incus_images.id = ? )
  ORDER BY incus_images.name
`)

var incusImageObjectsByName = RegisterStmt(`
SELECT incus_images.id, incus_images.name, incus_images.aliases, incus_images.description, incus_images.operating_system, incus_images.release, incus_images.architecture, incus_images.variant, incus_images.versions, incus_images.last_updated
  FROM incus_images
  WHERE ( incus_images.name = ? )
  ORDER BY incus_images.name
`)

var incusImageNames = RegisterStmt(`
SELECT incus_images.name
  FROM incus_images
  ORDER BY incus_images.name
`)

var incusImageID = RegisterStmt(`
SELECT incus_images.id FROM incus_images
  WHERE incus_images.name = ?
`)

var incusImageCreate = RegisterStmt(`
INSERT INTO incus_images (name, aliases, description, operating_system, release, architecture, variant, versions, last_updated)
  VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
`)

var incusImageUpdate = RegisterStmt(`
UPDATE incus_images
  SET name = ?, aliases = ?, description = ?, operating_system = ?, release = ?, architecture = ?, variant = ?, versions = ?, last_updated = ?
 WHERE id = ?
`)

var incusImageDeleteByName = RegisterStmt(`
DELETE FROM incus_images WHERE name = ?
`)

// GetIncusImageID return the ID of the incus_image with the given key.
// generator: incus_image ID
func GetIncusImageID(ctx context.Context, db tx, name string) (_ int64, _err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image")
	}()

	stmt, err := Stmt(db, incusImageID)
	if err != nil {
		return -1, fmt.Errorf("Failed to get \"incusImageID\" prepared statement: %w", err)
	}

	row := stmt.QueryRowContext(ctx, name)
	var id int64
	err = row.Scan(&id)
	if errors.Is(err, sql.ErrNoRows) {
		return -1, ErrNotFound
	}

	if err != nil {
		return -1, fmt.Errorf("Failed to get \"incus_images\" ID: %w", err)
	}

	return id, nil
}

// IncusImageExists checks if a incus_image with the given key exists.
// generator: incus_image Exists
func IncusImageExists(ctx context.Context, db dbtx, name string) (_ bool, _err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image")
	}()

	stmt, err := Stmt(db, incusImageID)
	if err != nil {
		return false, fmt.Errorf("Failed to get \"incusImageID\" prepared statement: %w", err)
	}

	row := stmt.QueryRowContext(ctx, name)
	var id int64
	err = row.Scan(&id)
	if errors.Is(err, sql.ErrNoRows) {
		return false, nil
	}

	if err != nil {
		return false, fmt.Errorf("Failed to get \"incus_images\" ID: %w", err)
	}

	return true, nil
}

// GetIncusImage returns the incus_image with the given key.
// generator: incus_image GetOne
func GetIncusImage(ctx context.Context, db dbtx, name string) (_ *image.IncusImage, _err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image")
	}()

	filter := image.IncusImageFilter{}
	filter.Name = &name

	objects, err := GetIncusImages(ctx, db, filter)
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"incus_images\" table: %w", err)
	}

	switch len(objects) {
	case 0:
		return nil, ErrNotFound
	case 1:
		return &objects[0], nil
	default:
		return nil, fmt.Errorf("More than one \"incus_images\" entry matches")
	}
}

// incusImageColumns returns a string of column names to be used with a SELECT statement for the entity.
// Use this function when building statements to retrieve database entries matching the IncusImage entity.
func incusImageColumns() string {
	return "incus_images.id, incus_images.name, incus_images.aliases, incus_images.description, incus_images.operating_system, incus_images.release, incus_images.architecture, incus_images.variant, incus_images.versions, incus_images.last_updated"
}

// getIncusImages can be used to run handwritten sql.Stmts to return a slice of objects.
func getIncusImages(ctx context.Context, stmt *sql.Stmt, args ...any) ([]image.IncusImage, error) {
	objects := make([]image.IncusImage, 0)

	dest := func(scan func(dest ...any) error) error {
		i := image.IncusImage{}
		var aliasesStr string
		err := scan(&i.ID, &i.Name, &aliasesStr, &i.Description, &i.OperatingSystem, &i.Release, &i.Architecture, &i.Variant, &i.Versions, &i.LastUpdated)
		if err != nil {
			return err
		}

		err = unmarshalJSON(aliasesStr, &i.Aliases)
		if err != nil {
			return err
		}

		objects = append(objects, i)

		return nil
	}

	err := selectObjects(ctx, stmt, dest, args...)
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"incus_images\" table: %w", err)
	}

	return objects, nil
}

// getIncusImagesRaw can be used to run handwritten query strings to return a slice of objects.
func getIncusImagesRaw(ctx context.Context, db dbtx, sql string, args ...any) ([]image.IncusImage, error) {
	objects := make([]image.IncusImage, 0)

	dest := func(scan func(dest ...any) error) error {
		i := image.IncusImage{}
		var aliasesStr string
		err := scan(&i.ID, &i.Name, &aliasesStr, &i.Description, &i.OperatingSystem, &i.Release, &i.Architecture, &i.Variant, &i.Versions, &i.LastUpdated)
		if err != nil {
			return err
		}

		err = unmarshalJSON(aliasesStr, &i.Aliases)
		if err != nil {
			return err
		}

		objects = append(objects, i)

		return nil
	}

	err := scan(ctx, db, sql, dest, args...)
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"incus_images\" table: %w", err)
	}

	return objects, nil
}

// GetIncusImages returns all available incus_images.
// generator: incus_image GetMany
func GetIncusImages(ctx context.Context, db dbtx, filters ...image.IncusImageFilter) (_ []image.IncusImage, _err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image")
	}()

	var err error

	// Result slice.
	objects := make([]image.IncusImage, 0)

	// Pick the prepared statement and arguments to use based on active criteria.
	var sqlStmt *sql.Stmt
	args := []any{}
	queryParts := [2]string{}

	if len(filters) == 0 {
		sqlStmt, err = Stmt(db, incusImageObjects)
		if err != nil {
			return nil, fmt.Errorf("Failed to get \"incusImageObjects\" prepared statement: %w", err)
		}
	}

	for i, filter := range filters {
		if filter.Name != nil && filter.ID == nil && filter.OperatingSystem == nil && filter.Release == nil && filter.Architecture == nil && filter.Variant == nil {
			args = append(args, []any{filter.Name}...)
			if len(filters) == 1 {
				sqlStmt, err = Stmt(db, incusImageObjectsByName)
				if err != nil {
					return nil, fmt.Errorf("Failed to get \"incusImageObjectsByName\" prepared statement: %w", err)
				}

				break
			}

			query, err := StmtString(incusImageObjectsByName)
			if err != nil {
				return nil, fmt.Errorf("Failed to get \"incusImageObjects\" prepared statement: %w", err)
			}

			parts := strings.SplitN(query, "ORDER BY", 2)
			if i == 0 {
				copy(queryParts[:], parts)
				continue
			}

			_, where, _ := strings.Cut(parts[0], "WHERE")
			queryParts[0] += "OR" + where
		} else if filter.ID != nil && filter.Name == nil && filter.OperatingSystem == nil && filter.Release == nil && filter.Architecture == nil && filter.Variant == nil {
			args = append(args, []any{filter.ID}...)
			if len(filters) == 1 {
				sqlStmt, err = Stmt(db, incusImageObjectsByID)
				if err != nil {
					return nil, fmt.Errorf("Failed to get \"incusImageObjectsByID\" prepared statement: %w", err)
				}

				break
			}

			query, err := StmtString(incusImageObjectsByID)
			if err != nil {
				return nil, fmt.Errorf("Failed to get \"incusImageObjects\" prepared statement: %w", err)
			}

			parts := strings.SplitN(query, "ORDER BY", 2)
			if i == 0 {
				copy(queryParts[:], parts)
				continue
			}

			_, where, _ := strings.Cut(parts[0], "WHERE")
			queryParts[0] += "OR" + where
		} else if filter.ID == nil && filter.Name == nil && filter.OperatingSystem == nil && filter.Release == nil && filter.Architecture == nil && filter.Variant == nil {
			return nil, fmt.Errorf("Cannot filter on empty IncusImageFilter")
		} else {
			return nil, errors.New("No statement exists for the given Filter")
		}
	}

	// Select.
	if sqlStmt != nil {
		objects, err = getIncusImages(ctx, sqlStmt, args...)
	} else {
		queryStr := strings.Join(queryParts[:], "ORDER BY")
		objects, err = getIncusImagesRaw(ctx, db, queryStr, args...)
	}

	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"incus_images\" table: %w", err)
	}

	return objects, nil
}

// GetIncusImageNames returns the identifying field of incus_image.
// generator: incus_image GetNames
func GetIncusImageNames(ctx context.Context, db dbtx, filters ...image.IncusImageFilter) (_ []string, _err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image")
	}()

	var err error

	// Result slice.
	names := make([]string, 0)

	// Pick the prepared statement and arguments to use based on active criteria.
	var sqlStmt *sql.Stmt
	args := []any{}
	queryParts := [2]string{}

	if len(filters) == 0 {
		sqlStmt, err = Stmt(db, incusImageNames)
		if err != nil {
			return nil, fmt.Errorf("Failed to get \"incusImageNames\" prepared statement: %w", err)
		}
	}

	for _, filter := range filters {
		if filter.ID == nil && filter.Name == nil && filter.OperatingSystem == nil && filter.Release == nil && filter.Architecture == nil && filter.Variant == nil {
			return nil, fmt.Errorf("Cannot filter on empty IncusImageFilter")
		} else {
			return nil, errors.New("No statement exists for the given Filter")
		}
	}

	// Select.
	var rows *sql.Rows
	if sqlStmt != nil {
		rows, err = sqlStmt.QueryContext(ctx, args...)
	} else {
		queryStr := strings.Join(queryParts[:], "ORDER BY")
		rows, err = db.QueryContext(ctx, queryStr, args...)
	}

	if err != nil {
		return nil, err
	}

	defer func() { _ = rows.Close() }()
	for rows.Next() {
		var identifier string
		err := rows.Scan(&identifier)
		if err != nil {
			return nil, err
		}

		names = append(names, identifier)
	}

	err = rows.Err()
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"incus_images\" table: %w", err)
	}

	return names, nil
}

// CreateIncusImage adds a new incus_image to the database.
// generator: incus_image Create
func CreateIncusImage(ctx context.Context, db dbtx, object image.IncusImage) (_ int64, _err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image")
	}()

	args := make([]any, 9)

	// Populate the statement arguments.
	args[0] = object.Name
	marshaledAliases, err := marshalJSON(object.Aliases)
	if err != nil {
		return -1, err
	}

	args[1] = marshaledAliases
	args[2] = object.Description
	args[3] = object.OperatingSystem
	args[4] = object.Release
	args[5] = object.Architecture
	args[6] = object.Variant
	args[7] = object.Versions
	args[8] = time.Now().UTC().Format(time.RFC3339)

	// Prepared statement to use.
	stmt, err := Stmt(db, incusImageCreate)
	if err != nil {
		return -1, fmt.Errorf("Failed to get \"incusImageCreate\" prepared statement: %w", err)
	}

	// Execute the statement.
	result, err := stmt.Exec(args...)
	if err != nil && strings.HasPrefix(err.Error(), "UNIQUE constraint failed:") {
		return -1, ErrConflict
	}

	if err != nil {
		return -1, fmt.Errorf("Failed to create \"incus_images\" entry: %w", err)
	}

	id, err := result.LastInsertId()
	if err != nil {
		return -1, fmt.Errorf("Failed to fetch \"incus_images\" entry ID: %w", err)
	}

	return id, nil
}

// UpdateIncusImage updates the incus_image matching the given key parameters.
// generator: incus_image Update
func UpdateIncusImage(ctx context.Context, db tx, name string, object image.IncusImage) (_err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image")
	}()

	id, err := GetIncusImageID(ctx, db, name)
	if err != nil {
		return err
	}

	stmt, err := Stmt(db, incusImageUpdate)
	if err != nil {
		return fmt.Errorf("Failed to get \"incusImageUpdate\" prepared statement: %w", err)
	}

	marshaledAliases, err := marshalJSON(object.Aliases)
	if err != nil {
		return err
	}

	result, err := stmt.Exec(object.Name, marshaledAliases, object.Description, object.OperatingSystem, object.Release, object.Architecture, object.Variant, object.Versions, time.Now().UTC().Format(time.RFC3339), id)
	if err != nil {
		return fmt.Errorf("Update \"incus_images\" entry failed: %w", err)
	}

	n, err := result.RowsAffected()
	if err != nil {
		return fmt.Errorf("Fetch affected rows: %w", err)
	}

	if n != 1 {
		return fmt.Errorf("Query updated %d rows instead of 1", n)
	}

	return nil
}

// DeleteIncusImage deletes the incus_image matching the given key parameters.
// generator: incus_image DeleteOne-by-Name
func DeleteIncusImage(ctx context.Context, db dbtx, name string) (_err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image")
	}()

	stmt, err := Stmt(db, incusImageDeleteByName)
	if err != nil {
		return fmt.Errorf("Failed to get \"incusImageDeleteByName\" prepared statement: %w", err)
	}

	result, err := stmt.Exec(name)
	if err != nil {
		return fmt.Errorf("Delete \"incus_images\": %w", err)
	}

	n, err := result.RowsAffected()
	if err != nil {
		return fmt.Errorf("Fetch affected rows: %w", err)
	}

	if n == 0 {
		return ErrNotFound
	} else if n > 1 {
		return fmt.Errorf("Query deleted %d IncusImage rows instead of 1", n)
	}

	return nil
}
