// Code generated by generate-database from the incus project - DO NOT EDIT.

package entities

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
	"time"

	"github.com/FuturFusion/operations-center/internal/image"
)

var incusImageSourceObjects = RegisterStmt(`
SELECT incus_image_sources.id, incus_image_sources.name, incus_image_sources.url, incus_image_sources.filter_expression, incus_image_sources.last_updated
  FROM incus_image_sources
  ORDER BY incus_image_sources.name
`)

var incusImageSourceObjectsByID = RegisterStmt(`
SELECT incus_image_sources.id, incus_image_sources.name, incus_image_sources.url, incus_image_sources.filter_expression, incus_image_sources.last_updated
  FROM incus_image_sources
  WHERE ( incus_image_sources.id = ? )
  ORDER BY incus_image_sources.name
`)

var incusImageSourceObjectsByName = RegisterStmt(`
SELECT incus_image_sources.id, incus_image_sources.name, incus_image_sources.url, incus_image_sources.filter_expression, incus_image_sources.last_updated
  FROM incus_image_sources
  WHERE ( incus_image_sources.name = ? )
  ORDER BY incus_image_sources.name
`)

var incusImageSourceNames = RegisterStmt(`
SELECT incus_image_sources.name
  FROM incus_image_sources
  ORDER BY incus_image_sources.name
`)

var incusImageSourceID = RegisterStmt(`
SELECT incus_image_sources.id FROM incus_image_sources
  WHERE incus_image_sources.name = ?
`)

var incusImageSourceCreate = RegisterStmt(`
INSERT INTO incus_image_sources (name, url, filter_expression, last_updated)
  VALUES (?, ?, ?, ?)
`)

var incusImageSourceUpdate = RegisterStmt(`
UPDATE incus_image_sources
  SET name = ?, url = ?, filter_expression = ?, last_updated = ?
 WHERE id = ?
`)

var incusImageSourceDeleteByName = RegisterStmt(`
DELETE FROM incus_image_sources WHERE name = ?
`)

// GetIncusImageSourceID return the ID of the incus_image_source with the given key.
// generator: incus_image_source ID
func GetIncusImageSourceID(ctx context.Context, db tx, name string) (_ int64, _err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image_source")
	}()

	stmt, err := Stmt(db, incusImageSourceID)
	if err != nil {
		return -1, fmt.Errorf("Failed to get \"incusImageSourceID\" prepared statement: %w", err)
	}

	row := stmt.QueryRowContext(ctx, name)
	var id int64
	err = row.Scan(&id)
	if errors.Is(err, sql.ErrNoRows) {
		return -1, ErrNotFound
	}

	if err != nil {
		return -1, fmt.Errorf("Failed to get \"incus_images_sources\" ID: %w", err)
	}

	return id, nil
}

// IncusImageSourceExists checks if a incus_image_source with the given key exists.
// generator: incus_image_source Exists
func IncusImageSourceExists(ctx context.Context, db dbtx, name string) (_ bool, _err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image_source")
	}()

	stmt, err := Stmt(db, incusImageSourceID)
	if err != nil {
		return false, fmt.Errorf("Failed to get \"incusImageSourceID\" prepared statement: %w", err)
	}

	row := stmt.QueryRowContext(ctx, name)
	var id int64
	err = row.Scan(&id)
	if errors.Is(err, sql.ErrNoRows) {
		return false, nil
	}

	if err != nil {
		return false, fmt.Errorf("Failed to get \"incus_images_sources\" ID: %w", err)
	}

	return true, nil
}

// GetIncusImageSource returns the incus_image_source with the given key.
// generator: incus_image_source GetOne
func GetIncusImageSource(ctx context.Context, db dbtx, name string) (_ *image.IncusImageSource, _err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image_source")
	}()

	filter := image.IncusImageSourceFilter{}
	filter.Name = &name

	objects, err := GetIncusImageSources(ctx, db, filter)
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"incus_images_sources\" table: %w", err)
	}

	switch len(objects) {
	case 0:
		return nil, ErrNotFound
	case 1:
		return &objects[0], nil
	default:
		return nil, fmt.Errorf("More than one \"incus_images_sources\" entry matches")
	}
}

// incusImageSourceColumns returns a string of column names to be used with a SELECT statement for the entity.
// Use this function when building statements to retrieve database entries matching the IncusImageSource entity.
func incusImageSourceColumns() string {
	return "incus_images_sources.id, incus_images_sources.name, incus_images_sources.url, incus_images_sources.filter_expression, incus_images_sources.last_updated"
}

// getIncusImageSources can be used to run handwritten sql.Stmts to return a slice of objects.
func getIncusImageSources(ctx context.Context, stmt *sql.Stmt, args ...any) ([]image.IncusImageSource, error) {
	objects := make([]image.IncusImageSource, 0)

	dest := func(scan func(dest ...any) error) error {
		i := image.IncusImageSource{}
		err := scan(&i.ID, &i.Name, &i.URL, &i.FilterExpression, &i.LastUpdated)
		if err != nil {
			return err
		}

		objects = append(objects, i)

		return nil
	}

	err := selectObjects(ctx, stmt, dest, args...)
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"incus_images_sources\" table: %w", err)
	}

	return objects, nil
}

// getIncusImageSourcesRaw can be used to run handwritten query strings to return a slice of objects.
func getIncusImageSourcesRaw(ctx context.Context, db dbtx, sql string, args ...any) ([]image.IncusImageSource, error) {
	objects := make([]image.IncusImageSource, 0)

	dest := func(scan func(dest ...any) error) error {
		i := image.IncusImageSource{}
		err := scan(&i.ID, &i.Name, &i.URL, &i.FilterExpression, &i.LastUpdated)
		if err != nil {
			return err
		}

		objects = append(objects, i)

		return nil
	}

	err := scan(ctx, db, sql, dest, args...)
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"incus_images_sources\" table: %w", err)
	}

	return objects, nil
}

// GetIncusImageSources returns all available incus_image_sources.
// generator: incus_image_source GetMany
func GetIncusImageSources(ctx context.Context, db dbtx, filters ...image.IncusImageSourceFilter) (_ []image.IncusImageSource, _err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image_source")
	}()

	var err error

	// Result slice.
	objects := make([]image.IncusImageSource, 0)

	// Pick the prepared statement and arguments to use based on active criteria.
	var sqlStmt *sql.Stmt
	args := []any{}
	queryParts := [2]string{}

	if len(filters) == 0 {
		sqlStmt, err = Stmt(db, incusImageSourceObjects)
		if err != nil {
			return nil, fmt.Errorf("Failed to get \"incusImageSourceObjects\" prepared statement: %w", err)
		}
	}

	for i, filter := range filters {
		if filter.Name != nil && filter.ID == nil {
			args = append(args, []any{filter.Name}...)
			if len(filters) == 1 {
				sqlStmt, err = Stmt(db, incusImageSourceObjectsByName)
				if err != nil {
					return nil, fmt.Errorf("Failed to get \"incusImageSourceObjectsByName\" prepared statement: %w", err)
				}

				break
			}

			query, err := StmtString(incusImageSourceObjectsByName)
			if err != nil {
				return nil, fmt.Errorf("Failed to get \"incusImageSourceObjects\" prepared statement: %w", err)
			}

			parts := strings.SplitN(query, "ORDER BY", 2)
			if i == 0 {
				copy(queryParts[:], parts)
				continue
			}

			_, where, _ := strings.Cut(parts[0], "WHERE")
			queryParts[0] += "OR" + where
		} else if filter.ID != nil && filter.Name == nil {
			args = append(args, []any{filter.ID}...)
			if len(filters) == 1 {
				sqlStmt, err = Stmt(db, incusImageSourceObjectsByID)
				if err != nil {
					return nil, fmt.Errorf("Failed to get \"incusImageSourceObjectsByID\" prepared statement: %w", err)
				}

				break
			}

			query, err := StmtString(incusImageSourceObjectsByID)
			if err != nil {
				return nil, fmt.Errorf("Failed to get \"incusImageSourceObjects\" prepared statement: %w", err)
			}

			parts := strings.SplitN(query, "ORDER BY", 2)
			if i == 0 {
				copy(queryParts[:], parts)
				continue
			}

			_, where, _ := strings.Cut(parts[0], "WHERE")
			queryParts[0] += "OR" + where
		} else if filter.ID == nil && filter.Name == nil {
			return nil, fmt.Errorf("Cannot filter on empty IncusImageSourceFilter")
		} else {
			return nil, errors.New("No statement exists for the given Filter")
		}
	}

	// Select.
	if sqlStmt != nil {
		objects, err = getIncusImageSources(ctx, sqlStmt, args...)
	} else {
		queryStr := strings.Join(queryParts[:], "ORDER BY")
		objects, err = getIncusImageSourcesRaw(ctx, db, queryStr, args...)
	}

	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"incus_images_sources\" table: %w", err)
	}

	return objects, nil
}

// GetIncusImageSourceNames returns the identifying field of incus_image_source.
// generator: incus_image_source GetNames
func GetIncusImageSourceNames(ctx context.Context, db dbtx, filters ...image.IncusImageSourceFilter) (_ []string, _err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image_source")
	}()

	var err error

	// Result slice.
	names := make([]string, 0)

	// Pick the prepared statement and arguments to use based on active criteria.
	var sqlStmt *sql.Stmt
	args := []any{}
	queryParts := [2]string{}

	if len(filters) == 0 {
		sqlStmt, err = Stmt(db, incusImageSourceNames)
		if err != nil {
			return nil, fmt.Errorf("Failed to get \"incusImageSourceNames\" prepared statement: %w", err)
		}
	}

	for _, filter := range filters {
		if filter.ID == nil && filter.Name == nil {
			return nil, fmt.Errorf("Cannot filter on empty IncusImageSourceFilter")
		} else {
			return nil, errors.New("No statement exists for the given Filter")
		}
	}

	// Select.
	var rows *sql.Rows
	if sqlStmt != nil {
		rows, err = sqlStmt.QueryContext(ctx, args...)
	} else {
		queryStr := strings.Join(queryParts[:], "ORDER BY")
		rows, err = db.QueryContext(ctx, queryStr, args...)
	}

	if err != nil {
		return nil, err
	}

	defer func() { _ = rows.Close() }()
	for rows.Next() {
		var identifier string
		err := rows.Scan(&identifier)
		if err != nil {
			return nil, err
		}

		names = append(names, identifier)
	}

	err = rows.Err()
	if err != nil {
		return nil, fmt.Errorf("Failed to fetch from \"incus_images_sources\" table: %w", err)
	}

	return names, nil
}

// CreateIncusImageSource adds a new incus_image_source to the database.
// generator: incus_image_source Create
func CreateIncusImageSource(ctx context.Context, db dbtx, object image.IncusImageSource) (_ int64, _err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image_source")
	}()

	args := make([]any, 4)

	// Populate the statement arguments.
	args[0] = object.Name
	args[1] = object.URL
	args[2] = object.FilterExpression
	args[3] = time.Now().UTC().Format(time.RFC3339)

	// Prepared statement to use.
	stmt, err := Stmt(db, incusImageSourceCreate)
	if err != nil {
		return -1, fmt.Errorf("Failed to get \"incusImageSourceCreate\" prepared statement: %w", err)
	}

	// Execute the statement.
	result, err := stmt.Exec(args...)
	if err != nil && strings.HasPrefix(err.Error(), "UNIQUE constraint failed:") {
		return -1, ErrConflict
	}

	if err != nil {
		return -1, fmt.Errorf("Failed to create \"incus_images_sources\" entry: %w", err)
	}

	id, err := result.LastInsertId()
	if err != nil {
		return -1, fmt.Errorf("Failed to fetch \"incus_images_sources\" entry ID: %w", err)
	}

	return id, nil
}

// UpdateIncusImageSource updates the incus_image_source matching the given key parameters.
// generator: incus_image_source Update
func UpdateIncusImageSource(ctx context.Context, db tx, name string, object image.IncusImageSource) (_err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image_source")
	}()

	id, err := GetIncusImageSourceID(ctx, db, name)
	if err != nil {
		return err
	}

	stmt, err := Stmt(db, incusImageSourceUpdate)
	if err != nil {
		return fmt.Errorf("Failed to get \"incusImageSourceUpdate\" prepared statement: %w", err)
	}

	result, err := stmt.Exec(object.Name, object.URL, object.FilterExpression, time.Now().UTC().Format(time.RFC3339), id)
	if err != nil {
		return fmt.Errorf("Update \"incus_images_sources\" entry failed: %w", err)
	}

	n, err := result.RowsAffected()
	if err != nil {
		return fmt.Errorf("Fetch affected rows: %w", err)
	}

	if n != 1 {
		return fmt.Errorf("Query updated %d rows instead of 1", n)
	}

	return nil
}

// DeleteIncusImageSource deletes the incus_image_source matching the given key parameters.
// generator: incus_image_source DeleteOne-by-Name
func DeleteIncusImageSource(ctx context.Context, db dbtx, name string) (_err error) {
	defer func() {
		_err = mapErr(_err, "Incus_image_source")
	}()

	stmt, err := Stmt(db, incusImageSourceDeleteByName)
	if err != nil {
		return fmt.Errorf("Failed to get \"incusImageSourceDeleteByName\" prepared statement: %w", err)
	}

	result, err := stmt.Exec(name)
	if err != nil {
		return fmt.Errorf("Delete \"incus_images_sources\": %w", err)
	}

	n, err := result.RowsAffected()
	if err != nil {
		return fmt.Errorf("Fetch affected rows: %w", err)
	}

	if n == 0 {
		return ErrNotFound
	} else if n > 1 {
		return fmt.Errorf("Query deleted %d IncusImageSource rows instead of 1", n)
	}

	return nil
}
